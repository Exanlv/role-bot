import { CommandConfig } from '@classes/command-config';

export class CommandConfigs {
	public readonly dev: CommandConfig[];
	public readonly admin: CommandConfig[];
	public readonly public: CommandConfig[];
}
